<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Oops music</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/main.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg ">
          <a href="/">
            <div class="header-info-logo-img">
                    <img class="logo" src="/images/logo1.png " alt="Music Services">
            </div>
          <a class="navbar-brand" href="/">Oops!</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <form class="d-flex" action="/search" method="POST">
            <input class="form-control me-2" type="search" name="searchBar" placeholder="Search songs by title & artist" aria-label="Search">
            <button style="background-color: #671E8B; color: white; border-color: #FFE71A;" class="btn btn-outline-success" type="submit">Search</button>
          </form>
          <div class="sidenav">
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
              <ul>
                  <a class="nav-link" href="/prints">All Song</a>
                <% if (!currentUser) { %>
                  <a class="nav-link" href="/login">SIGN IN</a>
                  <a class="nav-link" href="/register">SIGN UP</a>
                <% } else { %>
                  <a class="nav-link" href="/user/<%= currentUser._id %>">Profile</a>
                  <a class="nav-link" href="/showartist">Artist</a>
                  <a class="nav-link" href="/logout">Log Out</a>
                <% } %>
              </ul>
            </div>
          </div>
      </nav>
      <% if(error && error.length>0){ %>
        <div class= 'alert alert-danger' role = 'alert'>
          <%=  error %>
        </div>
      <% } %>
      <% if(success && success.length>0){ %>
        <div class= 'alert alert-success' role = 'alert'>
          <%=  success %>
        </div>
      <% } %>

      
  
    